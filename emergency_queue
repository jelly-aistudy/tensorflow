## input 
5 2 #n,m
60 50 70 80 90 #risk

## question
Bring out the first patient from the waiting list.
If there is another patient who has higher risk, put the patient to the end of the waiting list. Else, get a treatment.
Print out when mth patient would get treatment.

import sys
from collections import deque
sys.stdin=open("input.txt","rt")

n,m=map(int, input().split())
a=list(map(int, input().split()))
a=deque(a)
cnt=0

while True:
    if m!=0:
        if a[0]==max(a):
            a.popleft()
            cnt+=1
        else:
            a.append(a.popleft())
        m-=1
    else:
        if a[0]==max(a):
            cnt+=1
            print(cnt)
            break
        else:
            a.append(a.popleft())
            m=len(a)-1
